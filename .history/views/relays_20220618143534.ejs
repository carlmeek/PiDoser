<%- include('includes/header.ejs',{active:'relays'}) %>
<div class="container">
    <% for (const [key,probe] of Object.entries(params.probes)) { %>
        <div class="card">
            <div class="card-header">
            <%=probe.title%> - <%=probe.desc%>
            </div>
            <div class="card-body">
            <%<%-this.relayState%>
            </div>
        </div>
        <Br> 
    <% } %>
</div>
<%- include('includes/footer.ejs') %>


