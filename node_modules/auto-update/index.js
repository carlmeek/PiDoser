var fs = require('fs')
var unzip = require('unzip')
var async = require('async')
var get = require('./get')

async.waterfall([
    function(next) {
      // githubからzipで取得
      get()
      console.log("githubからzipで取得")
      next()
    },
    function(next){
      //zipをカレントディレクトリに解凍
      console.log('zipをカレントディレクトリに解凍')  
      next()
    }
  ])